# Source models

Place the original PC-side training outputs here, for example:

```text
xgboost_model.json
lightgbm_model.txt
feature_schema.json
```

Large or sensitive training artifacts should normally remain outside Git.
They are inputs to the converter, not files read directly by the firmware.

